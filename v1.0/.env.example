##############################################
# 🛠️ REQUIRED FOR FIRST-TIME SETUP (setup.sh) #
##############################################

# System timezone to set on the backup VM
TIMEZONE="America/Los_Angeles"

# Your GitHub username (used to pull SSH public keys)
GITHUB_USERNAME="your-github-username"

# DNS configuration (used to set persistent DNS)
DNS="8.8.8.8 1.1.1.1"
FALLBACK_DNS="9.9.9.9 1.0.0.1"

# Enable and configure Tailscale VPN (optional)
USE_TAILSCALE_AUTH_KEY=true
TAILSCALE_AUTH_KEY="tskey-auth-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"


###########################################################
# ⏬ REQUIRED AFTER SETUP: FOR BACKUP/IMPORT SCRIPTS       #
###########################################################

# Backup target host (use your backup server’s Tailscale IP or DNS)
BACKUP_HOST="ubuntu@your.remote.ip"   # Fill in after Tailscale setup
SSH_KEY="/home/ubuntu/.ssh/id_rsa"    # Path to SSH key from source server

# Cron schedule for running backup_mirror.sh (on source server)
CRON_SCHEDULE="0 2 * * *"

# MariaDB credentials and dump path (used by backup_mirror.sh)
DB_USER="yourdbuser"
DB_PASSWORD="yourdbpassword"
DB_HOST="localhost"
DB_DUMP_FILE="/home/ubuntu/backups/db_dumps/mariadb_backup.sql"

# Folders and services to sync (used on both servers)
FOLDERS=("velocity" "mc1" "mc2" "mc3")
SERVICES=("velocity" "mc1" "mc2" "mc3")

# Optional Discord webhook notifications
ENABLE_DISCORD=true
ENABLE_DISCORD_IMPORT=true
DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/your_webhook_url"

# Optional config rewrites during restore (import.sh)
# Format: /path/to/file;find_this;replace_with
CONFIG_UPDATES=(
  "/home/ubuntu/mc1/plugins/ItemsAdder/config.yml;oldip;newip"
  "/home/ubuntu/mc2/plugins/ItemsAdder/config.yml;oldpack.url;newpack.url"
  "/home/ubuntu/mc3/plugins/ItemsAdder/config.yml;oldpack.url;newpack.url"
  "/home/ubuntu/velocity/velocity.toml;oldmcserver.url;newmcserver.url"
)

# Feature toggles
ENABLE_FOLDERS=true
ENABLE_SERVICES=true
ENABLE_DATABASE=true
ENABLE_REMOTE_IMPORT=true
RESTART_SERVICES=true
